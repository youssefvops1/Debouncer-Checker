# Debouncer-Checker
This Python script is a multi-threaded bulk email validator designed to check large lists of email addresses efficiently and automatically. It sends verification requests to a remote validation API and classifies emails as Valid or Dead based on the server response.
